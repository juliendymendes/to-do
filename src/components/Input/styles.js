import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	input: {
		width: '100%',
		backgroundColor: '#FFF',
		padding: 25,
		borderRadius: 40,
		fontSize: 18,
		marginVertical:20
	},
})

export default styles